# Workbench UI 改进 PRD

> 版本：v2（2026-03-06 改进）
> 原始想法来自 0306_improved_prd.md（v1）
> 状态：待确认后执行

---

## 总览

本次改进涉及 Workbench 的 5 个区域，按优先级排列：

| 区域           | 改动类型                | 复杂度 |
| -------------- | ----------------------- | ------ |
| [1] 学习资源区 | 按钮改名 + 新增弹窗     | 低     |
| [2] 学习任务区 | 样式修复 + 交互简化     | 中     |
| [3] 聊天区     | 3 处 UI 重构            | 高     |
| [4] Header 栏  | 配置重组 + 发布弹窗改版 | 高     |
| [5] 学习状态区 | 颜色规范 + 空状态       | 低     |
| [6] 笔记栏     | 新增保存功能            | 中     |

---

## [1] 学习资源区

**文件：** `workbench/resource/LeftPanel.tsx` L247–279

### 1-A 按钮改名

**问题：** 大虚线按钮标签"添加资料来源"含义模糊，点击后实际是文件上传弹窗。

**修改：**

- 大虚线按钮：label 从 `添加资料来源` 改为 `上传文件`，功能不变（仍触发 `onFileUploadOpen`）
- 三个小按钮原有"上传文件"小按钮删除（因为大按钮已承担），替换为"粘贴文本"

**改后三个小按钮排列：**

```
[从资源库导入]  [粘贴链接]  [粘贴文本]
```

### 1-B 新增"粘贴文本"功能

**触发方式：** 点击"粘贴文本"小按钮。

**交互：** 打开一个新弹窗（新建 `PasteTextModal.tsx`），内容：

- 顶部标题："粘贴文本"
- 多行文本输入框（`<textarea>`，高度约 200px，placeholder："在此粘贴文字内容…"）
- 底部两个按钮：
  - 取消
  - `确定添加`：将文本内容作为一条新资源添加到学习资源列表，资源 type 为 `text`，title 取前 20 个字符，弹窗关闭

---

## [2] 学习任务区

**文件：** `workbench/resource/LeftPanel.tsx`

### 2-A 已完成任务样式修复

**问题：** 已完成任务和未完成任务在视觉上区分不明显，绿色小标签"第N次"不够直观。

**目标效果（参考 NotebookLM 风格）：**

- 已完成任务的标题文字加删除线（`line-through`）
- 整条任务卡片文字颜色降低为灰色（`text-gray-400`），整体 opacity 降低（`opacity-70`）
- 左侧圆形状态图标：从空圆圈改为实心绿色勾（`CheckCircle2` 图标，绿色）
- "第N次"绿色标签保留，但改为更小、更内敛的样式（不带背景色，仅灰色文字 `text-xs text-gray-400`）
- "重做"按钮改为图标按钮（`RotateCcw` 图标），hover 时显示 tooltip "重做"

**具体修改位置：**

- 任务卡片 className 中根据 `completedTasks.has(task.id)` 条件添加 `opacity-70`
- 任务标题 `<span>` 添加条件 className `line-through text-gray-400`
- 状态图标由 `Circle` 替换为 `CheckCircle2`（已完成时）

### 2-B 任务区按钮简化

**问题：** 任务区头部有"生成任务"按钮，底部有"AI 生成更多任务"按钮，但 AI 生成入口已在右侧学习工具区存在，造成入口重复且混乱。

**修改：**

- 删除任务区头部的"生成任务"按钮（`LeftPanel.tsx` L490–510）
- 删除任务列表底部的"AI 生成更多任务"虚线按钮（`LeftPanel.tsx` L684–700）
- 保留/新增一个"手动添加任务"的 `+` 图标按钮（在任务区 header 右侧），点击后打开任务编辑弹窗（`TaskEditModal`，已有）

**改后任务区头部：**

```
学习任务  [+]
```

AI 生成任务的唯一入口：右侧学习工具区的工具按钮。

### 2-C 任务生成整体交互重新梳理（⚠️ 需要单独设计确认）

**问题描述：** 当前流程层级过深：

```
点击学习工具 → 个性化配置弹窗 → 生成任务 → 任务卡片上又有2个配置弹窗（内含AI生成）
```

**建议新流程（待确认）：**

```
点击学习工具（如"生成测验"）
  → 右侧 inline 展开配置面板（不弹窗，直接在工具按钮下方展开）
  → 配置完成点"生成"
  → 任务出现在左侧任务列表
  → 点击任务标题可展开内联编辑（不弹窗）
```

> ⚠️ **待确认**：这个流程是否符合预期？特别是"任务配置"应该在哪里发生——是生成前配置，还是生成后编辑？

---

## [3] 中间聊天区

**文件：** `workbench/chat/ChatPanel.tsx`

### 3-A 聊天中的任务卡片改为折叠样式

**问题：** 任务卡片以 `displayMode="embedded"` 展示完整做题界面，占据大量聊天空间，使对话上下文被推远。

**目标：** 任务在聊天流中仅显示为一个**折叠卡片**，点击后进入全屏做题。

**折叠卡片样式（新建或修改 `TaskExpandedCard` 的 embedded 模式）：**

```
┌─────────────────────────────────────┐
│ [📝图标]  分数应用题练习              │
│           选择题 · 5题 · 预计10分钟   │
│                          [开始做题→] │
└─────────────────────────────────────┘
```

- 卡片高度固定约 72px，不展开题目内容
- 状态标签：未开始 / 进行中 / 已完成（`bg-green-100 text-green-700`）
- 已完成状态：显示"查看结果"按钮，点击进入 `result_review` 模式

**修改位置：** `ChatPanel.tsx` L258–291，将 `<TaskExpandedCard>` 替换为新的 `<TaskCollapsedCard>` 组件（新建 `task/TaskCollapsedCard.tsx`）。

### 3-B 功能按钮与推荐回复二选一

**问题：** `actionButtons`（功能按钮）和 `quickReplies`（推荐回复）可能同时出现，导致选项过多。

**规则：** 优先显示推荐回复；有推荐回复时，隐藏功能按钮。

**修改位置：** `ChatPanel.tsx` L207–307，将两处条件渲染改为：

```
如果 quickReplies.length > 0：只显示 quickReplies
否则：如果 actionButtons.length > 0：只显示 actionButtons
```

即将 `actionButtons` 的显示条件从：

```js
message.suggestions?.actionButtons?.length > 0
```

改为：

```js
message.suggestions?.actionButtons?.length > 0 && !message.suggestions?.quickReplies?.length
```

### 3-C 功能按钮改为组件卡片样式

**问题：** 当前功能按钮（`actionButtons`）是小型横排按钮，信息密度低，视觉不够清晰。

**目标：** 改为带图标、标题、说明文字的卡片样式，每张卡片独占一行。参考下方布局：

```
┌─────────────────────────────────────┐
│ [🔬]  生成知识检测题                  │
│       根据本节内容生成 5 道选择题       │
└─────────────────────────────────────┘
┌─────────────────────────────────────┐
│ [📊]  生成思维导图                    │
│       梳理本章核心概念的逻辑结构        │
└─────────────────────────────────────┘
```

**具体修改：**

- `ChatPanel.tsx` L207–250，将按钮的 className 从小横排按钮改为竖排卡片
- 每个卡片：`rounded-xl border border-gray-200 bg-white px-4 py-3 flex items-start gap-3 hover:border-primary-300 hover:bg-primary-50`
- 图标区：左侧 40×40 的圆形背景 `bg-primary-50 rounded-xl`，图标居中
- 文字区：上行 `font-medium text-sm text-gray-800`（label），下行 `text-xs text-gray-500`（description，来自 `button.description` 字段）
- 需要在 mock 数据的 `actionButtons` 中补充 `description` 字段

### 3-D 聊天中加入学习进度节点卡片（⚠️ 待明确需求）

**原始描述：** "聊天中也需要加入这种"（参考截图 1772784172834）

> ⚠️ **需要明确：** 截图中的内容是什么？是学习路径节点切换的提示卡片，还是学习进度汇总卡片，还是别的？请补充说明后再设计。

---

## [4] Header 栏

**文件：** `workbench/header/WorkbenchHeader.tsx`、`components/SettingsModal.tsx`、`components/PublishModal.tsx`

### 4-A 配置弹窗：消除弹窗套弹窗

**问题：** 点击"设置"打开 `SettingsModal`，在 SettingsModal 内点击"AI交互配置"又弹出子 Modal（`FreeModeConfigModal` / `GuidedModeConfigModal` / `MetaConfigModal`），弹窗套弹窗体验差。

**目标：** 所有配置在一个界面内完成，子配置项用**折叠展开（Accordion）**或**内嵌 Panel**替代弹窗。

**改法：**

将 SettingsModal 内的三个子 Modal 入口改为 inline 展开区块：

```
设置 Modal
├── 基础配置（直接显示）
│   ├── 学习方式：[自由探索] [AI引导]
│   └── 学习目标：[textarea]
├── AI 交互配置（点击展开 ▼）
│   ├── 自由对话模式配置（inline，原 FreeModeConfigModal 内容）
│   └── 引导学习模式配置（inline，原 GuidedModeConfigModal 内容）
├── 笔记与监控（点击展开 ▼）
│   ├── 笔记模板
│   └── 学情监控（inline，原 MetaConfigModal 内容）
└── 高级配置（点击展开 ▼）
    └── …
```

**修改方式：** 在 `SettingsModal.tsx` 中将三处"点击打开子 Modal"的按钮，替换为 accordion 组件（可用 `<details>`/`<summary>` 或自定义 toggle state），将子 Modal 的 JSX 内容内联进来，不再使用独立弹窗。

原子 Modal 文件（`FreeModeConfigModal.tsx` / `GuidedModeConfigModal.tsx` / `MetaConfigModal.tsx`）保留但标记为废弃，后续可删除。

### 4-B 发布成功状态加二维码

**问题：** 当前发布成功后仅显示文字链接和访问码，没有二维码，分享不便。

**目标：** 发布成功状态新增二维码展示，参考微信分享弹窗风格（保持绿色主题）。

**布局参考：**

```
┌──────────────────────────────────┐
│  ✅ 发布成功                       │
│                                  │
│  ┌────────┐  学习空间已发布         │
│  │  二维码  │  链接：[复制] ←───── │
│  │  图片   │  访问码：XXXX         │
│  └────────┘                      │
│                                  │
│  [复制链接]        [完成]          │
└──────────────────────────────────┘
```

**技术实现：**

- 在 `PublishModal.tsx` 的发布成功状态中，使用 `qrcode.react` 或 `react-qr-code` 库生成二维码
- 二维码内容为分享链接（已有 `shareLink` 变量）
- 二维码尺寸：120×120px，颜色：绿色（`#16a34a`）或黑色均可

---

## [5] 学习状态区

**文件：** `workbench/workspace/LearningStatusPanel.tsx`

### 5-A 学习路径节点颜色规范

**问题：** 当前颜色仅区分"当前/已完成/未开始"状态，无法区分活动类型。

**目标：** 按**活动类型**分配固定颜色，使人一眼识别：

| 活动类型                  | 颜色 | Tailwind 参考                                       |
| ------------------------- | ---- | --------------------------------------------------- |
| 学习内容（阅读/观看资源） | 蓝色 | `bg-blue-100 text-blue-700 border-blue-200`       |
| 完成任务（做题/提交）     | 绿色 | `bg-green-100 text-green-700 border-green-200`    |
| AI 主场（AI 引导/讲解）   | 紫色 | `bg-purple-100 text-purple-700 border-purple-200` |
| 其他（过渡/说明节点）     | 灰色 | `bg-gray-100 text-gray-600 border-gray-200`       |

**修改位置：** `LearningStatusPanel.tsx` 中 `learningPath.map()` 部分，根据 `node.type`（需确认字段名）动态选择颜色 className，而不是根据 `node.status`。

> ⚠️ **待确认：** 学习路径节点 `LearningPathNode` 是否有 `type` 字段区分活动类型？若无，需先在 `types.ts` 和 mock 数据中补充。

### 5-B 空状态与收起功能

**问题：** 当学生刚进入空间时，学习状态区无数据但仍占据空间，且该区域无法收起。

**两种处理方案（选一）：**

**方案 A（推荐）——空状态占位图：**

- 当 `learningPath.length === 0` 时，显示一个空状态占位：
  ```
  ┌─────────────────────────┐
  │  📊                     │
  │  开始学习后               │
  │  这里将显示学习进度         │
  └─────────────────────────┘
  ```
- 不需要收起功能，空状态时也保持区域可见

**方案 B——面板可收起：**

- 在"学习状态"标题右侧加一个 `ChevronUp/Down` 图标按钮
- 点击后收起面板，整个 `RightPanel` 的工作区标签下方只显示笔记区
- 收起状态持久化到 `localStorage`

> ⚠️ **待确认：** 选择方案 A 还是方案 B？

---

## [6] 笔记栏

**文件：** `workbench/workspace/EnhancedNotesPanel.tsx`

### 6-A 新增"保存到笔记"功能

**原始描述：** "添加增加到 source / 保存到笔记栏"

> ⚠️ **需要明确：** 这个功能的具体触发点在哪里？

可能的两种理解，请确认哪个：

**理解 A：** 在聊天消息上增加"保存到笔记"按钮

- 每条 AI 消息 hover 时，右上角出现"📌 保存"图标
- 点击后将消息内容以引用块形式追加到当前笔记（或新建笔记）

**理解 B：** 在学习资源上增加"添加到资源"按钮

- 在笔记编辑器底部加一个"发布为学习资源"按钮
- 点击后将当前笔记内容作为一条新资源添加到左侧学习资源列表

---

## 待确认事项汇总

在开始开发前，以下问题需要你确认：

| #  | 问题                                                                                | 影响区域 |
| -- | ----------------------------------------------------------------------------------- | -------- |
| Q1 | "粘贴文本"弹窗中"保存"和"确定"是不同操作，还是合并为一个按钮？                      | [1-B]    |
| Q2 | 任务生成新流程（工具按钮 inline 展开配置）是否符合预期？                            | [2-C]    |
| Q3 | 聊天中要加入的"进度节点卡片"具体是什么样的？                                        | [3-D]    |
| Q4 | 学习路径节点是否已有 `type` 字段区分活动类型？                                    | [5-A]    |
| Q5 | 学习状态区：空状态占位（方案A）还是可收起（方案B）？                                | [5-B]    |
| Q6 | "保存到笔记"功能的触发点：聊天消息 hover 保存（理解A）还是笔记发布为资源（理解B）？ | [6-A]    |
