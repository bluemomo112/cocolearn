# 自习室交互式 Demo 改造方案

## 一、用户视角：用户会看到什么？

### 1.1 初始状态（空白页面）
- 用户进入自习室后，看到的是一个**干净的空白界面**
- 左侧资源区：空的，显示"暂无资源"提示
- 中间对话区：空的，只有输入框和四个入口按钮
  - 📤 文件上传
  - 🔗 从链接导入
  - 📚 从知识库导入
  - ➕ 其他方式
- 右侧任务区：空的，显示"暂无任务"提示

### 1.2 触发演示（用户操作后）
用户点击任何一个入口按钮或在输入框输入内容后：
- **渐进式内容呈现**：内容不是一次性全部出现，而是像真实对话一样逐条显示
- **打字机效果**：AI 回复以打字机效果逐字显示
- **资源/任务动态添加**：随着对话推进，资源和任务逐个"飞入"左右两侧面板
- **引导式交互**：AI 会主动提问、引导用户进行下一步操作

### 1.3 不同场景的演示流程

#### 场景 A：上传任意文件
1. 用户点击"文件上传"
2. 弹出文件选择器（演示模式下，任何文件都可以）
3. 上传后，AI 欢迎语出现："我看到你上传了一份关于【XX】的资料，让我帮你分析一下..."
4. 用户输入任何内容（如"好的"、"开始吧"），触发预设脚本
5. AI 按脚本逐步输出：
   - 分析文件内容
   - 提取关键知识点
   - 生成学习路径
   - 创建相关任务
6. 每个步骤都伴随着资源/任务的动态添加

#### 场景 B：上传 Quiz（空白试卷）
1. 用户上传 PDF/图片格式的试卷
2. AI 识别："检测到这是一份测试卷，正在转换为互动试卷..."
3. 自动转换为可交互的题目卡片
4. 添加到任务区，用户可以直接答题

#### 场景 C：批量上传答题记录
1. 用户上传多张学生答题卡图片（如 20 张）
2. AI 显示识别进度："正在识别答题记录... 1/20"
3. 识别完成后显示："识别到 20 份答题记录，来自【MIT 期中考试】"
4. 生成 20 个学生的个人答题报告，存入资源区
5. AI 询问："需要我帮你分析全班的错题分布吗？"

#### 场景 D：从知识库导入
1. 用户点击"从知识库导入"
2. 弹出选择界面：
   - 历史测试记录（老师可选全班/个人，学生只能选个人）
   - 错题本（只能选自己的）
3. 选择后，相关资源导入到资源区
4. AI 开始引导学习流程

## 二、系统运作：背后是如何工作的？

### 2.1 演示脚本系统
```
演示脚本 = {
  场景类型: "任意文件上传" | "Quiz上传" | "批量答题记录" | "知识库导入",
  触发条件: "用户操作",
  脚本步骤: [
    { 延迟: 1000ms, 类型: "AI消息", 内容: "...", 附加动作: "添加资源" },
    { 延迟: 2000ms, 类型: "AI消息", 内容: "...", 附加动作: "添加任务" },
    ...
  ]
}
```

### 2.2 状态机管理
```
初始状态: "空白"
  ↓ 用户操作
进入状态: "演示中"
  ↓ 脚本执行
  - 逐步添加消息
  - 逐步添加资源
  - 逐步添加任务
  ↓ 脚本结束
最终状态: "演示完成"（可继续自由对话）
```

### 2.3 数据流
1. **用户操作** → 触发场景识别
2. **场景识别** → 加载对应脚本
3. **脚本执行器** → 按时间轴播放脚本
4. **UI 更新** → 渐进式渲染内容
5. **用户输入** → 推进脚本或自由对话

### 2.4 用户角色区分
- **老师用户**：
  - 可以导入全班的答题记录
  - 可以看到全班的统计分析
- **学生用户**：
  - 只能导入自己的答题记录
  - 只能看到自己的错题本

## 三、实现方案对比

### 方案 A：完全脚本化（推荐）

**核心思路**：预设多个演示脚本，用户操作后按脚本播放

**优点**：
- 演示效果可控，体验流畅
- 实现相对简单，易于调试
- 可以精心设计每个演示场景
- 性能好，无需真实 AI 调用

**缺点**：
- 灵活性较低，用户输入不影响脚本
- 需要编写多个脚本

**实现步骤**：
1. 创建脚本数据结构（JSON 格式）
2. 实现脚本执行引擎（定时器 + 状态管理）
3. 实现打字机效果组件
4. 实现资源/任务动态添加动画
5. 为每个场景编写演示脚本
6. 添加场景识别逻辑

**涉及文件**：
- `SelfStudyWorkbench.tsx`（主组件，添加脚本执行逻辑）
- `demoScripts.ts`（新建，存放所有演示脚本）
- `ScriptEngine.tsx`（新建，脚本执行引擎）
- `TypingMessage.tsx`（新建，打字机效果组件）

---

### 方案 B：半脚本化 + 真实交互

**核心思路**：脚本提供主线，但允许用户输入影响部分内容

**优点**：
- 兼顾演示效果和交互灵活性
- 用户感觉更真实
- 可以根据用户输入调整部分内容

**缺点**：
- 实现复杂度高
- 需要处理用户输入的各种情况
- 可能出现演示流程被打断的情况

**实现步骤**：
1. 实现方案 A 的所有步骤
2. 添加用户输入解析逻辑
3. 实现脚本分支系统（根据用户输入选择不同分支）
4. 添加回退机制（用户输入不符合预期时的处理）

**涉及文件**：
- 方案 A 的所有文件
- `InputParser.ts`（新建，解析用户输入）
- `ScriptBranching.ts`（新建，脚本分支逻辑）

---

### 方案 C：完全动态生成

**核心思路**：根据用户操作实时生成演示内容（需要真实 AI）

**优点**：
- 最灵活，可以应对任何用户输入
- 真实的 AI 体验

**缺点**：
- 需要真实的 AI 后端支持
- 演示效果不可控
- 性能开销大
- 不适合纯前端 Demo

**不推荐理由**：
- 你的需求是"演示用的 Demo"，不需要真实 AI
- 方案 A 已经可以满足需求

## 四、推荐方案详细设计（方案 A）

### 4.1 脚本数据结构
```typescript
interface DemoScript {
  id: string;
  name: string;
  trigger: 'file_upload' | 'link_import' | 'knowledge_base' | 'any_input';
  fileType?: 'any' | 'quiz' | 'batch_answers';
  steps: DemoStep[];
}

interface DemoStep {
  delay: number; // 延迟时间（ms）
  type: 'ai_message' | 'add_resource' | 'add_task' | 'update_path' | 'wait_user_input';
  content?: string; // AI 消息内容
  resourceData?: Resource; // 要添加的资源
  taskData?: Task; // 要添加的任务
  pathUpdate?: LearningPathNode[]; // 学习路径更新
  typingSpeed?: number; // 打字速度（字符/秒）
}
```

### 4.2 核心组件

#### 4.2.1 ScriptEngine（脚本执行引擎）
- 负责按时间轴执行脚本步骤
- 管理执行状态（播放/暂停/停止）
- 触发 UI 更新

#### 4.2.2 TypingMessage（打字机效果）
- 逐字显示 AI 消息
- 可配置打字速度
- 支持 Markdown 渲染

#### 4.2.3 AnimatedResource/Task（动画组件）
- 资源/任务添加时的飞入动画
- 使用 CSS transition 或 Framer Motion

### 4.3 演示脚本示例

#### 脚本 1：任意文件上传
```typescript
{
  id: 'demo_any_file',
  name: '任意文件上传演示',
  trigger: 'file_upload',
  fileType: 'any',
  steps: [
    {
      delay: 500,
      type: 'ai_message',
      content: '我看到你上传了一份关于【植物工厂】的资料，让我帮你分析一下...',
      typingSpeed: 30
    },
    {
      delay: 2000,
      type: 'ai_message',
      content: '分析完成！我从资料中提取了以下关键知识点：\n\n1. 植物工厂的定义与特点\n2. 环境控制技术\n3. 光照系统设计\n4. 营养液配方\n\n我已经为你生成了学习路径，你可以按照路径逐步学习。',
      typingSpeed: 30
    },
    {
      delay: 1000,
      type: 'add_resource',
      resourceData: {
        id: 'res_demo_1',
        title: '植物工厂核心概念',
        type: 'document',
        // ... 其他字段
      }
    },
    {
      delay: 500,
      type: 'add_resource',
      resourceData: {
        id: 'res_demo_2',
        title: '环境控制技术详解',
        type: 'document',
        // ... 其他字段
      }
    },
    {
      delay: 1000,
      type: 'add_task',
      taskData: {
        id: 'task_demo_1',
        title: '基础概念测验',
        type: 'quiz',
        // ... 其他字段
      }
    },
    {
      delay: 1000,
      type: 'ai_message',
      content: '接下来你想做什么？\n\n1. 开始学习基础概念\n2. 直接做测验\n3. 让我推荐学习路径',
      typingSpeed: 30
    },
    {
      delay: 0,
      type: 'wait_user_input'
    }
  ]
}
```

### 4.4 实现关键点

1. **初始状态管理**
   - 添加 `isDemoMode` 状态
   - 添加 `demoScript` 状态（当前执行的脚本）
   - 添加 `demoStepIndex` 状态（当前执行到第几步）

2. **脚本触发**
   - 在文件上传、链接导入等操作时，检查是否为演示模式
   - 根据操作类型选择对应脚本
   - 启动脚本执行

3. **用户输入处理**
   - 演示模式下，用户输入主要用于推进脚本
   - 脚本执行到 `wait_user_input` 时，等待用户输入
   - 用户输入后，继续执行后续步骤

4. **退出演示模式**
   - 脚本执行完毕后，自动退出演示模式
   - 用户可以继续自由对话（此时不再按脚本执行）

## 五、实施计划

### 阶段 1：基础架构（2-3 小时）
- [ ] 创建脚本数据结构和类型定义
- [ ] 实现 ScriptEngine 组件
- [ ] 实现 TypingMessage 组件
- [ ] 添加演示模式状态管理

### 阶段 2：动画效果（1-2 小时）
- [ ] 实现资源/任务飞入动画
- [ ] 优化打字机效果
- [ ] 添加过渡动画

### 阶段 3：脚本编写（2-3 小时）
- [ ] 编写"任意文件上传"脚本
- [ ] 编写"Quiz 上传"脚本
- [ ] 编写"批量答题记录"脚本
- [ ] 编写"知识库导入"脚本

### 阶段 4：集成测试（1-2 小时）
- [ ] 测试各个场景的演示流程
- [ ] 调整时间和动画效果
- [ ] 修复 bug

### 阶段 5：用户角色区分（1 小时）
- [ ] 添加用户角色判断逻辑
- [ ] 根据角色显示不同的导入选项
- [ ] 测试老师/学生两种角色

**总计：7-11 小时**

## 六、需要确认的问题

1. **演示脚本的详细内容**：你希望每个场景的具体对话内容是什么？需要我根据现有的 mock 数据编写，还是你有具体的脚本文案？

2. **用户角色**：当前代码中有 `mode?: 'teacher' | 'student'` 参数，是否已经有用户角色的判断逻辑？还是需要我添加？

3. **演示模式的触发**：是否所有用户进入自习室都默认进入演示模式？还是需要一个开关来控制？

4. **脚本执行速度**：打字速度、延迟时间等参数，你有偏好吗？还是我按照常规的节奏设置？

5. **退出演示模式**：脚本执行完毕后，用户是否可以继续自由对话？还是需要重新开始？

请确认以上方案是否符合你的需求，以及需要我先实现哪个阶段。
