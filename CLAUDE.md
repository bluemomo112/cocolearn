# Cross-New 项目规则

## 项目概述
- 技术栈：TypeScript + React + Next.js
- 核心功能：自习室 UI 模块（学习任务、资源管理、笔记面板）
- 包管理：使用 npm

## 构建与类型检查
- 多文件修改后必须运行 `npm run build` 或 `tsc --noEmit` 验证
- 特别注意：
  - TaskStatus 枚举值的类型匹配
  - question type 字符串字面量的正确性
  - 组件 props 的必需属性

## TSX/JSX 文件编辑规则
- **大文件处理**（500+ 行）：
  - 禁止使用 sed 或 awk 修复括号/大括号
  - 必须先用 Read 读取错误位置前后 50-80 行代码
  - 理解完整的 JSX 嵌套结构后，使用 Edit 工具做精确修改
- **错误修复限制**：
  - 同一构建错误尝试 2 次后仍未解决，停止并展示相关代码块
  - 不要陷入反复修改括号的循环

## Mock 数据管理
- 插入 mock 数据前，确认目标数组名称：
  - `mockResources` - 学习资源数据
  - `mockTasks` - 学习任务数据
  - `mockQuestions` - 题目数据
- 验证插入位置与实现功能的匹配性

## UI 开发约束
- **"仅 UI" 或 "UI-only" 指令含义**：
  - 只修改组件代码、样式、JSX 结构
  - 不修改：类型定义、API 调用、数据层逻辑、mock 数据结构
- **修改流程**：
  - 修改完成后先展示 diff
  - 等待确认后再应用更改

## 工作流偏好
- **分阶段实现**：
  - 多阶段功能按阶段拆分，每阶段完成后 commit
  - 每个 commit 前确保构建通过
  - 等待确认后再开始下一阶段
- **范围控制**：
  - 严格遵守指定的实现方法（如 worktree 隔离、特定文件范围）
  - 不要超出请求范围添加额外功能或重构
  - 有疑问先询问，不要假设

## Git 操作
- Commit 消息必须描述实际变更内容，而非意图
- 使用 worktree 时，确保在正确的工作树中操作
