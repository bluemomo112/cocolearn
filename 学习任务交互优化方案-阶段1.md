# 学习任务交互优化方案 - 阶段1：核心交互逻辑重构

## 一、用户视角

### 用户会看到什么？

**1. 对话区的任务卡片**
- 用户点击任务后，对话区会出现一张任务卡片（类似聊天消息）
- 卡片显示任务标题、类型、当前进度
- 卡片有两种状态：
  - **嵌入式**：在对话区正常大小显示，可以看到题目和答题区
  - **全屏中**：卡片显示"正在全屏做题"的提示，显示当前题号和进度

**2. 全屏做题界面**
- 点击任务或卡片上的"全屏"按钮，弹出全屏overlay
- 全屏界面占满整个屏幕，沉浸式做题
- 顶部有"缩小"按钮，可以随时缩小回对话区
- 底部有题目导航，显示当前是第几题

**3. 状态同步**
- 在全屏做题时，对话区的卡片会实时显示当前进度（第几题）
- 缩小后，对话区卡片立即显示当前的答题状态：
  - 如果在第3题，卡片就显示第3题的内容和已选答案
  - 如果已完成，卡片显示完成状态和结果
- 点击卡片可以重新全屏继续做题，状态完全保持

### 用户如何操作？

**流程1：开始做题**
1. 用户点击任务列表中的某个任务
2. 对话区出现任务卡片（嵌入式）
3. 卡片自动展开为全屏模式
4. 用户开始答题

**流程2：全屏做题**
1. 在全屏界面选择答案
2. 点击"下一题"继续
3. 随时可以点击"缩小"回到对话区

**流程3：缩小查看历史**
1. 点击"缩小"按钮
2. 全屏界面消失
3. 对话区的卡片显示当前答题状态（保持在当前题号）
4. 可以滚动查看之前的学习记录
5. 点击卡片上的"全屏"按钮，重新进入全屏，从当前题号继续

**流程4：完成任务**
1. 答完所有题目
2. 提交后显示结果
3. 缩小后，对话区卡片显示完成状态和成绩
4. 这条记录永久保留在对话区

### 学习流程的连续性

对话区会形成这样的记录：
```
[用户消息] 我想学习线性代数
[AI回复] 好的，我为你准备了...
[任务卡片1] 线性代数基础测试 - 已完成 8/10题正确
[用户消息] 继续
[任务卡片2] 矩阵运算练习 - 进行中 3/5题
[AI回复] 你做得很好...
```

## 二、系统运作

### 数据流

**1. 任务打开流程**
```
用户点击任务
  ↓
创建任务消息对象（包含任务ID、初始状态）
  ↓
添加到消息列表
  ↓
设置 expandedTask = 任务对象
  ↓
设置 taskDisplayMode = 'fullscreen'
  ↓
渲染全屏overlay + 对话区卡片（显示"全屏中"提示）
```

**2. 答题状态更新流程**
```
用户选择答案
  ↓
更新 selectedAnswers 状态
  ↓
同步更新消息列表中的任务消息对象
  ↓
全屏界面和对话区卡片都从同一个数据源读取
  ↓
两边显示一致
```

**3. 缩小流程**
```
用户点击"缩小"
  ↓
设置 taskDisplayMode = 'embedded'
  ↓
全屏overlay消失
  ↓
对话区卡片从"全屏中"状态切换为"嵌入式"状态
  ↓
显示完整的题目和当前答题状态
```

**4. 重新全屏流程**
```
用户点击卡片上的"全屏"按钮
  ↓
设置 expandedTask = 该任务对象
  ↓
设置 taskDisplayMode = 'fullscreen'
  ↓
全屏overlay显示，从当前题号继续
```

### 关键组件协作

**1. 消息数据结构扩展**
```typescript
type Message = {
  id: string;
  type: 'user' | 'assistant' | 'task';  // 新增 task 类型
  content: string;
  timestamp: Date;

  // 任务消息专属字段
  taskData?: {
    taskId: string;
    task: Task;
    currentQuestionIndex: number;
    selectedAnswers: Record<string, string | string[]>;
    submissionText: string;
    status: 'idle' | 'in_progress' | 'completed';
    result?: {
      allCorrect: boolean;
      correctCount: number;
      totalCount: number;
    };
  };
};
```

**2. 状态管理**
```typescript
// 父组件状态
const [messages, setMessages] = useState<Message[]>([]);
const [expandedTask, setExpandedTask] = useState<Task | null>(null);
const [taskDisplayMode, setTaskDisplayMode] = useState<'fullscreen' | 'embedded'>('fullscreen');

// 当前任务的状态从 messages 中读取
const currentTaskMessage = messages.find(m =>
  m.type === 'task' && m.taskData?.taskId === expandedTask?.id
);
```

**3. 组件渲染逻辑**
```
SelfStudyWorkbench (父组件)
  ├─ 消息列表
  │   ├─ 用户消息
  │   ├─ AI消息
  │   └─ 任务卡片消息 (TaskCard组件)
  │       ├─ 嵌入式模式：显示完整题目和答题区
  │       └─ 全屏中模式：显示"正在全屏做题"提示
  │
  └─ 全屏Overlay (条件渲染)
      └─ TaskExpandedCard组件
          └─ 全屏模式UI
```

## 三、实现方案

### 主要步骤

**步骤1：扩展消息数据结构**
- 修改 Message 类型定义，添加 task 类型
- 添加 taskData 字段存储任务状态

**步骤2：创建 TaskCard 组件**
- 新建 TaskCard.tsx 组件
- 支持两种显示模式：
  - embedded：显示完整题目和答题区（复用 TaskExpandedCard 的嵌入式UI）
  - fullscreen_active：显示"正在全屏做题"提示
- 点击"全屏"按钮触发父组件的 openTask 函数

**步骤3：修改任务打开逻辑**
- 修改 openTask 函数：
  - 创建任务消息对象
  - 添加到 messages 列表
  - 设置 expandedTask 和 taskDisplayMode

**步骤4：修改状态更新逻辑**
- 所有答题状态更新（selectedAnswers, currentQuestionIndex等）都同步到 messages 中的任务消息
- TaskExpandedCard 和 TaskCard 都从 messages 读取状态

**步骤5：修改缩小逻辑**
- 修改 toggleTaskDisplayMode 函数：
  - 只改变 taskDisplayMode，不清空 expandedTask
  - 对话区的 TaskCard 根据 taskDisplayMode 切换显示模式

**步骤6：修改全屏渲染逻辑**
- 全屏 overlay 只在 taskDisplayMode === 'fullscreen' 时显示
- 使用 fixed + z-50 确保覆盖整个屏幕

### 涉及文件

**主要修改：**
1. `src/app/teacher/self-study/components/SelfStudyWorkbench.tsx`
   - 扩展消息类型
   - 修改状态管理
   - 修改任务打开/缩小逻辑
   - 修改渲染逻辑

**新建文件：**
2. `src/app/teacher/self-study/components/TaskCard.tsx`
   - 任务卡片组件
   - 支持嵌入式和全屏中两种模式

**可能需要修改：**
3. `src/types/self-study.ts` 或相关类型文件
   - 扩展 Message 类型定义

### 关键代码改动点

**1. SelfStudyWorkbench.tsx 状态管理部分（约行1326-1335）**
- 移除独立的 selectedAnswers, currentQuestionIndex 等状态
- 改为从 messages 中读取

**2. openTask 函数（约行1745）**
- 添加创建任务消息的逻辑

**3. toggleTaskDisplayMode 函数（约行1749-1751）**
- 修改逻辑，不清空状态

**4. TaskExpandedCard 组件（约行648-1300+）**
- 修改 props，接收 taskMessage 而不是独立的状态
- 状态更新时调用回调函数更新 messages

**5. 消息渲染部分（约行2800+）**
- 添加 task 类型消息的渲染逻辑
- 渲染 TaskCard 组件

## 四、实现优先级

### 核心功能（必须）
1. ✅ 消息数据结构扩展
2. ✅ TaskCard 组件创建
3. ✅ 任务打开时创建消息
4. ✅ 状态同步机制
5. ✅ 缩小后保持状态

### 增强功能（可选）
1. 动画过渡效果（全屏展开/缩小）
2. 卡片折叠/展开功能
3. 历史任务记录的持久化

## 五、预期效果

完成后：
- ✅ 用户点击任务，对话区出现卡片记录
- ✅ 全屏做题时，对话区卡片显示"全屏中"
- ✅ 缩小后，卡片显示当前答题状态（题号、答案都保持）
- ✅ 可以重新全屏继续做题
- ✅ 所有学习记录在对话区连续展示
- ✅ 全屏和嵌入式是同一个组件的两种展示模式

## 六、后续阶段预告

**阶段2：客观题反馈增强**
- 提交按钮
- 对错状态显示
- AI提示功能

**阶段3：题型展示丰富**
- 填空题输入框
- 拖动、匹配互动
- 公式、图片支持

**阶段4：主观题增强**
- 富媒体内容
- 多样输入方式
